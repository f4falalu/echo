import { BASE_URL_V2 } from '../config';

export const ELECTRIC_BASE_URL = `${BASE_URL_V2}/electric-shape`;

export const DEFAULT_UPDATE_OPERATIONS: Array<'insert' | 'update' | 'delete'> = ['update'];
