export enum VerificationStatus {
  notRequested = 'notRequested',
  requested = 'requested',
  inReview = 'inReview',
  verified = 'verified',
  backlogged = 'backlogged',
  notVerified = 'notVerified'
}
